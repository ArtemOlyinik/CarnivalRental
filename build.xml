<project name="CarnivalRental" default="compile" basedir=".">
  <property name="src.dir" value="src"/>
  <property name="build.dir" value="out/production/CarnivalRental"/>
  <property name="dist.dir" value="dist"/>
  <property name="lib.dir" value="lib"/>

  <path id="lib.path">
    <fileset dir="${lib.dir}">
      <include name="**/*.jar"/>
    </fileset>
  </path>

  <target name="clean">
    <delete dir="${dist.dir}"/>
    <delete dir="${build.dir}"/>
  </target>

  <target name="compile">
    <mkdir dir="${build.dir}"/>
    <javac srcdir="${src.dir}" destdir="${build.dir}" includeantruntime="false">
      <compilerarg value="--release"/>
      <compilerarg value="21"/>
      <classpath refid="lib.path"/>
    </javac>
  </target>

  <target name="jar" depends="compile">
    <mkdir dir="${dist.dir}"/>
    <jar destfile="${dist.dir}/CarnivalRental.jar" basedir="${build.dir}">
      <manifest>
        <attribute name="Main-Class" value="com.carnival.rental.presentation.Main"/>
      </manifest>
    </jar>
  </target>

  <target name="run" depends="compile">
    <echo message="Запускаємо сервіс прокату карнавальних костюмів..."/>
    <java classname="com.carnival.rental.presentation.Main" fork="true">
      <classpath>
        <path refid="lib.path"/>
        <pathelement location="${build.dir}"/>
      </classpath>
    </java>
  </target>
</project>